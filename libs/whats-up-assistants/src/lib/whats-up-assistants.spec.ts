import { whatsUpAssistants } from './whats-up-assistants';

describe('whatsUpAssistants', () => {
  it('should work', () => {
    expect(whatsUpAssistants()).toEqual('whats-up-assistants');
  });
});
